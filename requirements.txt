# Core web framework
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.2.1
python-dotenv>=1.0.1

# HTTP client (used by agents for OpenAI API calls and attack simulator)
httpx>=0.27.0

# Redis (event streaming between sensors and orchestrator)
redis>=5.0.4

# Monitoring / Sensors
psutil>=5.9.8          # optional — network sensor falls back gracefully without it
watchdog>=4.0.0

# Dashboard (optional enhanced UI — built-in dashboard at /dashboard needs no extras)
# pip install streamlit plotly pandas
# streamlit>=1.33.0
# plotly>=5.21.0
# pandas>=2.2.2

# Optional LLM upgrade: set OPENAI_API_KEY in .env to enable LLM-powered agents
# openai>=1.30.0       # used via direct httpx calls, no SDK needed

# Development / testing
# numpy>=1.26.4
# scikit-learn>=1.4.2
